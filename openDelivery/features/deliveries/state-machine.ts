import type {DeliveryStatus,Role} from "@/generated/prisma/enums";
const allowed:Record<DeliveryStatus,readonly DeliveryStatus[]>={AVAILABLE:["COURIER_SELECTED","CANCELLED_BY_BUSINESS"],COURIER_SELECTED:["COURIER_TO_PICKUP","CANCELLED_BY_BUSINESS","CANCELLED_BY_COURIER","DISPUTED"],COURIER_TO_PICKUP:["AT_PICKUP","CANCELLED_BY_BUSINESS","CANCELLED_BY_COURIER","DISPUTED"],AT_PICKUP:["PICKED_UP","CANCELLED_BY_BUSINESS","CANCELLED_BY_COURIER","DISPUTED"],PICKED_UP:["IN_TRANSIT","CANCELLED_BY_COURIER","DISPUTED"],IN_TRANSIT:["AT_DESTINATION","DISPUTED"],AT_DESTINATION:["DELIVERED","DISPUTED"],DELIVERED:["COMPLETED","DISPUTED"],COMPLETED:["DISPUTED"],CANCELLED_BY_BUSINESS:[],CANCELLED_BY_COURIER:[],DISPUTED:[]};
const courierOnly=new Set<DeliveryStatus>(["COURIER_TO_PICKUP","AT_PICKUP","PICKED_UP","IN_TRANSIT","AT_DESTINATION","DELIVERED","CANCELLED_BY_COURIER"]);
export function assertTransition(from:DeliveryStatus,to:DeliveryStatus,role:Role){if(!allowed[from].includes(to))throw new Error("INVALID_DELIVERY_TRANSITION");if(courierOnly.has(to)&&role!=="COURIER")throw new Error("FORBIDDEN_TRANSITION");if(to==="CANCELLED_BY_BUSINESS"&&role!=="BUSINESS")throw new Error("FORBIDDEN_TRANSITION");if((to==="COMPLETED"||to==="DISPUTED")&&!(["BUSINESS","COURIER","ADMIN"] as Role[]).includes(role))throw new Error("FORBIDDEN_TRANSITION");}
export function transitionsFrom(s:DeliveryStatus){return allowed[s];}
