"use client";import{useEffect}from"react";import{ui}from"@/locales/ui";
export function LocaleUi({locale}:{locale:string}){useEffect(()=>{if(locale!=="ar"&&locale!=="he")return;const map=ui[locale];const visit=(n:Node)=>{if(n.nodeType===3){const r=n.textContent||"",s=r.trim();if(map[s as keyof typeof map])n.textContent=r.replace(s,map[s as keyof typeof map])}if(n instanceof HTMLElement)for(const a of["placeholder","aria-label","title"]){const v=n.getAttribute(a);if(v&&map[v as keyof typeof map])n.setAttribute(a,map[v as keyof typeof map])}n.childNodes.forEach(visit)};visit(document.body);const o=new MutationObserver(rs=>rs.forEach(r=>r.addedNodes.forEach(visit)));o.observe(document.body,{subtree:true,childList:true});return()=>o.disconnect()},[locale]);return null}
