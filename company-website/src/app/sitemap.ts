import type { MetadataRoute } from "next";import { productSeeds } from "@/config/products";export default function sitemap():MetadataRoute.Sitemap{const base=process.env.APP_URL||'http://localhost:3000';const pages=['','/demos','/pricing','/about','/contact','/request-demo',...productSeeds.map(p=>`/products/${p.slug}`)];return ['en','ar','he'].flatMap(l=>pages.map(p=>({url:`${base}/${l}${p}`,lastModified:new Date()})))}
