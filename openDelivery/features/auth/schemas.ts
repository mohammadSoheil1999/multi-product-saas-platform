import {z} from "zod";
const base=z.object({name:z.string().trim().min(2).max(100),email:z.string().email().transform(x=>x.toLowerCase()),phone:z.string().regex(/^\+?[0-9 -]{7,20}$/),password:z.string().min(12).max(128),preferredLanguage:z.enum(["en","ar","he"]),termsAccepted:z.literal(true),privacyAccepted:z.literal(true),tenantSlug:z.string().default("default")});
export const registrationSchema=z.discriminatedUnion("role",[base.extend({role:z.literal("BUSINESS"),businessName:z.string().min(2).max(120),category:z.string().min(2).max(80),address:z.string().min(3).max(300),latitude:z.number().min(-90).max(90),longitude:z.number().min(-180).max(180),city:z.string().min(2).max(100),registrationNumber:z.string().max(80).optional()}),base.extend({role:z.literal("COURIER"),city:z.string().min(2).max(100),vehicleType:z.enum(["CAR","MOTORCYCLE","SCOOTER","BICYCLE","WALKING","OTHER"]),serviceAreaIds:z.array(z.string().cuid()).min(1),independentDeclarationAccepted:z.literal(true)})]);
