import {betterAuth} from "better-auth";import {prismaAdapter} from "better-auth/adapters/prisma";import argon2 from "argon2";import {db} from "@/lib/db";import {env} from "@/lib/env";
export const auth=betterAuth({database:prismaAdapter(db,{provider:"postgresql"}),secret:env.AUTH_SECRET,baseURL:env.APP_URL,emailAndPassword:{enabled:true,minPasswordLength:12,password:{hash:password=>argon2.hash(password,{type:argon2.argon2id,memoryCost:19456,timeCost:2,parallelism:1}),verify:({hash,password})=>argon2.verify(hash,password)}},emailVerification:{sendOnSignUp:false},trustedOrigins:[env.APP_URL],advanced:{useSecureCookies:env.NODE_ENV==="production"},user:{additionalFields:{tenantId:{type:"string",input:false},role:{type:"string",input:false},status:{type:"string",input:false},preferredLanguage:{type:"string",input:false}}}});
