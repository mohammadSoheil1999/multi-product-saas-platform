import {env} from "@/lib/env";import {logger} from "@/lib/logger";
export async function sendVerification(email:string,token:string,language:string):Promise<string|null>{const link=`${env.APP_URL}/${language}/verify-email?token=${encodeURIComponent(token)}&email=${encodeURIComponent(email)}`;if(env.NODE_ENV!=="production"&&!process.env.EMAIL_API_KEY){logger.info({email,verificationLink:link},"development verification email");return link}if(!process.env.EMAIL_API_KEY)throw new Error("EMAIL_NOT_CONFIGURED");const response=await fetch("https://api.resend.com/emails",{method:"POST",headers:{Authorization:`Bearer ${process.env.EMAIL_API_KEY}`,"Content-Type":"application/json"},body:JSON.stringify({from:process.env.EMAIL_FROM,to:email,subject:"Verify your OpenDelivery account",html:`<p>Verify your account:</p><p><a href="${link}">Verify email</a></p>`})});if(!response.ok)throw new Error("EMAIL_DELIVERY_FAILED");return null}
