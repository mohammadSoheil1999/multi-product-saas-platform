import { LoginForm } from "@/components/auth-forms";import { dictionaries,localeOf } from "@/lib/i18n";export const metadata={title:"Client Login"};export default async function Page({params}:{params:Promise<{locale:string}>}){const {locale}=await params;const t=dictionaries[localeOf(locale)].auth;return <div className="auth-shell"><div className="card auth-card"><div className="eyebrow">{t.portal}</div><h1>{t.welcome}</h1><p className="muted">{t.loginIntro}</p><LoginForm locale={locale}/></div></div>}
