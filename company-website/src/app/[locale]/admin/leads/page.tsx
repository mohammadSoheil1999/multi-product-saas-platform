import { db } from "@/lib/db";export default async function Page(){const rows=await db.lead.findMany({orderBy:{createdAt:'desc'},take:100});return <><div className="eyebrow">LEADS</div><h1>New business requests</h1>{rows.map(x=><div className="card" style={{padding:20,marginBottom:12}} key={x.id}><strong>{x.name} · {x.businessName}</strong><p className="muted">{x.type} · {x.productSlug||'General'} · {x.email} · {x.createdAt.toLocaleDateString()}</p><p>{x.message}</p></div>)}</>}
