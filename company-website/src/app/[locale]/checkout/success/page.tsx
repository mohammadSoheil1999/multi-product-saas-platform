import Link from "next/link";export default async function Page({params,searchParams}:{params:Promise<{locale:string}>;searchParams:Promise<Record<string,string>>}){const {locale}=await params;const pending=(await searchParams).pending==="1";return <div className="auth-shell"><div className="card auth-card"><div className="eyebrow">{pending?"REQUEST RECEIVED":"WELCOME"}</div><h1>{pending?"Your plan is awaiting activation.":"Your subscription is active."}</h1><p className="muted">{pending?"No payment was taken. We will review the request and prepare your product.":"Next: complete your business information, branding, domain, and customization requirements."}</p><Link className="button accent" href={`/${locale}/dashboard/products`}>View my products</Link></div></div>}
