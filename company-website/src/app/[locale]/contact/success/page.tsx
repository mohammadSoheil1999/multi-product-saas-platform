import Link from "next/link";export default async function Page({params}:{params:Promise<{locale:string}>}){const {locale}=await params;return <div className="auth-shell"><div className="card auth-card" style={{textAlign:'center'}}><div className="eyebrow">REQUEST RECEIVED</div><h1>Thank you.</h1><p className="muted">We’ve stored your request and will follow up using your preferred contact details.</p><Link className="button primary" href={`/${locale}`}>Back home</Link></div></div>}
