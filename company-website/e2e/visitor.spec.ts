import {test,expect} from "@playwright/test";test("visitor can explore products, demos, pricing and registration",async({page})=>{await page.goto("/en");await expect(page.getByRole("heading",{name:"Turn Your Business Into Software."})).toBeVisible();await page.getByRole("link",{name:/Try free demos/i}).click();await expect(page.getByRole("heading",{name:"Try Before You Buy."})).toBeVisible();await page.goto("/en/pricing");await expect(page.getByText("Professional").first()).toBeVisible();await page.goto("/en/register");await expect(page.getByRole("heading",{name:"Create your account."})).toBeVisible()});
