import {VerifyEmail} from "@/components/auth/VerifyEmail";
export default async function VerifyPage({params,searchParams}:{params:Promise<{locale:string}>;searchParams:Promise<{token?:string;email?:string}>}){const {locale}=await params;const {token="",email=""}=await searchParams;return <main className="container section"><div className="logo">Open<span>Delivery</span></div><h1>Account verification</h1>{token&&email?<VerifyEmail token={token} email={email} locale={locale}/>:<div className="card"><h2>Invalid verification link</h2><p>The token or email address is missing.</p></div>}</main>}
