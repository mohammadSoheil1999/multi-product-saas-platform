import { RegisterForm } from "@/components/auth-forms";import { dictionaries,localeOf } from "@/lib/i18n";export const metadata={title:"Create Account"};export default async function Page({params}:{params:Promise<{locale:string}>}){const {locale}=await params;const t=dictionaries[localeOf(locale)].auth;return <div className="auth-shell"><div className="card auth-card"><div className="eyebrow">{t.startPlatform}</div><h1>{t.createTitle}</h1><p className="muted">{t.createIntro}</p><RegisterForm locale={locale}/></div></div>}
