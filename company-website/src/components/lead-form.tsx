import { submitLead } from "@/app/actions";import { productSeeds } from "@/config/products";
export function LeadForm({type="CONTACT"}:{type?:"CONTACT"|"DEMO"|"CUSTOM_DEVELOPMENT"}){return <form action={submitLead} className="form card" style={{padding:30}}><input type="hidden" name="type" value={type}/><div className="grid grid-2"><label className="field">Name<input name="name" required/></label><label className="field">Business name<input name="businessName"/></label></div><div className="grid grid-2"><label className="field">Email<input name="email" type="email" required/></label><label className="field">Phone<input name="phone" type="tel"/></label></div><label className="field">Interested product<select name="productSlug"><option value="">Choose a product</option>{productSeeds.map(p=><option value={p.slug} key={p.slug}>{p.name}</option>)}</select></label>{type==="DEMO"&&<label className="field">Preferred contact method<select name="preferredContact"><option>Email</option><option>Phone</option><option>WhatsApp</option></select></label>}<label className="field">Message<textarea name="message" rows={6} required/></label><label><input name="consent" type="checkbox" required/> I agree to be contacted about this request.</label><button className="button accent">{type==="DEMO"?'Request demo':'Request consultation'}</button></form>}
