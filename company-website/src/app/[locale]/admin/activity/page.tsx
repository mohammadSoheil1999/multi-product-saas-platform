import {db} from "@/lib/db";
export default async function Page(){const rows=await db.auditLog.findMany({include:{actor:true},orderBy:{createdAt:"desc"},take:250});return <><div className="eyebrow">OWNER AUDIT TRAIL</div><h1>Activity log</h1><p className="muted">The latest 250 administrative and commercial events.</p><div className="card" style={{overflowX:"auto"}}><table style={{width:"100%",borderCollapse:"collapse"}}><thead><tr>{["Time","Action","Actor","Target","Details"].map(x=><th key={x} style={{padding:12,textAlign:"start"}}>{x}</th>)}</tr></thead><tbody>{rows.map(row=><tr key={row.id} style={{borderTop:"1px solid var(--line)"}}><td style={{padding:12}}>{row.createdAt.toLocaleString()}</td><td>{row.action}</td><td>{row.actor?.email??"System"}</td><td>{row.targetType}{row.targetId?` · ${row.targetId}`:""}</td><td><code>{JSON.stringify(row.metadata)}</code></td></tr>)}</tbody></table></div></>}
