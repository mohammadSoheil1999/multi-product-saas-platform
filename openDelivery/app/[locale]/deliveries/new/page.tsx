import {requireBusiness} from "@/features/auth/authorization";import {DeliveryForm} from "@/components/DeliveryForm";export default async function NewDelivery(){await requireBusiness();return <main className="container section"><div className="logo">Open<span>Delivery</span></div><h1>Find a courier</h1><p>Create an opportunity for independent couriers. Nobody is automatically assigned.</p><DeliveryForm/></main>}
