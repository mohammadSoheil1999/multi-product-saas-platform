import { requireUser } from "@/lib/authorization";export default async function Page(){const s=await requireUser();return <><div className="eyebrow">PROFILE</div><h1>Account information</h1><form className="form card" style={{padding:25,maxWidth:650}}><label className="field">Full name<input defaultValue={s.user.name}/></label><label className="field">Email<input value={s.user.email} disabled/></label><label className="field">Preferred language<select defaultValue={(s.user as {preferredLanguage?:string}).preferredLanguage}><option value="en">English</option><option value="ar">Arabic</option><option value="he">Hebrew</option></select></label><button className="button accent">Save changes</button></form></>}
