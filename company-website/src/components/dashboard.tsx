import Link from "next/link";import type { Locale } from "@/lib/i18n";
export function DashboardShell({locale,admin=false,children}:{locale:Locale;admin?:boolean;children:React.ReactNode}){const client=[["Overview",""],["My Products","/products"],["Subscriptions","/subscriptions"],["Billing","/billing"],["Requests","/requests"],["Support","/support"],["Profile","/settings/profile"],["Security","/settings/security"]];const adminLinks=[["Overview",""],["Clients","/clients"],["Services, Plans & Demos","/products"],["Memberships","/subscriptions"],["Requests","/requests"],["Support","/support"],["Site Content","/content"],["Leads","/leads"],["Activity Log","/activity"]];const base=`/${locale}/${admin?'admin':'dashboard'}`;return <div className="dashboard"><aside className="sidebar"><div className="eyebrow">{admin?'OWNER CONTROL':'CLIENT PORTAL'}</div><nav style={{marginTop:20}}>{(admin?adminLinks:client).map(([label,path])=><Link key={label} href={`${base}${path}`}>{label}</Link>)}</nav></aside><div className="dash-main">{children}</div></div>}
export function Stat({label,value,detail}:{label:string;value:string|number;detail?:string}){return <div className="card stat"><span className="muted">{label}</span><strong>{value}</strong>{detail&&<small className="muted">{detail}</small>}</div>}
